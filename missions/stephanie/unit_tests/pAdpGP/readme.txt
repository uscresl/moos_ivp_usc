To run:
pAntler pGPTest.moos

This should show 2 windows, one for pLogger (which is generating log files of the MOOSDB content), 
and one for pGP, which shows whatever output this process is producing.
