To run:
pAntler pTemplateTest.moos

This should show 2 windows, one for pLogger (which is generating log files of the MOOSDB content), 
and one for pTemplate, which shows whatever output this process is producing.
